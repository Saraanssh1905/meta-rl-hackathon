[project]
name = "incident-triage-env"
version = "1.0.0"
description = "IT incident triage OpenEnv environment"
requires-python = ">=3.10"

dependencies = [
    "fastapi==0.110.0",
    "uvicorn[standard]==0.29.0",
    "pydantic==2.6.4",
    "openai",
    "huggingface-hub",
]